(defwidget powermenu [Halign]
(box :class "powermenu" :orientation "v"
    :halign Halign
    (button :class "buttons reboot" :onclick "reboot" :tooltip "Reboot" "")
    (button :class "buttons lock" :onclick "dm-tool lock" :tooltip "Lock" "")
    (button :class "buttons poweroff" :onclick "poweroff" :tooltip "Poweroff" "")
)
)
